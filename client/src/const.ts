// Standalone version — Manus OAuth removed.
// This app uses a licence key system (see client/src/lib/licence.ts).
// No login URL is required for the current auth model.

export const COOKIE_NAME = "insync_session";
export const ONE_YEAR_MS = 365 * 24 * 60 * 60 * 1000;
